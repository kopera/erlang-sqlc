{erl_opts, [debug_info]}.
{deps, [
]}.

{xrl_opts, [
    {error_location, column}
]}.
{yrl_opts, [
    {error_location, column}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [sqlc]}
]}.
